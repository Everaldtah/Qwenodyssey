{
  "suite": "qwen_odyssey_coder_limits",
  "target_model": "qwen2.5-coder-7b",
  "version": "1.0",
  "tests": [
    {"id":"T01_fizzbuzz_twist","difficulty":1,"prompt":"Write a Python function `fizzbuzz(n)` returning a list of strings for 1..n inclusive. Multiples of 3 -> 'Fizz', of 5 -> 'Buzz', of both -> 'FizzBuzz', of 7 -> 'Bazz' (7 takes priority over 3 and 5 when it applies, but a number that is a multiple of 7 AND 3 AND 5 is still 'FizzBuzz'). Otherwise the number as a string. Return only the code.","check":"assert fizzbuzz(7) == ['1','2','Fizz','4','Buzz','Fizz','Bazz']\nassert fizzbuzz(105)[104] == 'FizzBuzz'\nassert fizzbuzz(21)[20] == 'Bazz'\nassert fizzbuzz(35)[34] == 'Bazz'\nassert fizzbuzz(15)[14] == 'FizzBuzz'"},
    {"id":"T02_word_reverse","difficulty":1,"prompt":"Write `reverse_words(s)` that reverses the order of words but preserves each word's internal characters, collapsing runs of whitespace to a single space and stripping leading/trailing space. Return only the code.","check":"assert reverse_words('  hello   world  ') == 'world hello'\nassert reverse_words('a') == 'a'\nassert reverse_words('') == ''\nassert reverse_words('one two three') == 'three two one'"},
    {"id":"T03_fib_memo","difficulty":1,"prompt":"Write `fib(n)` returning the n-th Fibonacci number (fib(0)=0, fib(1)=1) using memoization that does NOT use a mutable default argument and does NOT leak state across calls. Must handle n=70 fast. Return only the code.","check":"assert fib(0)==0 and fib(1)==1 and fib(10)==55\nassert fib(70)==190392490709135\nimport re\nsigs=''.join(re.findall(r'def\\s+\\w+\\s*\\(([^)]*)\\)', SOURCE)).replace(' ','')\nassert '={}' not in sigs and '=[]' not in sigs and '=dict()' not in sigs and '=list()' not in sigs"},
    {"id":"T04_binary_search","difficulty":2,"prompt":"Write `search_insert(nums, target)` for a sorted list with possible duplicates: return the index of the FIRST occurrence of target, or the index where it would be inserted to keep sorted order if absent. O(log n). Return only the code.","check":"assert search_insert([1,3,3,3,5],3)==1\nassert search_insert([1,3,5],4)==2\nassert search_insert([],1)==0\nassert search_insert([2,2,2],2)==0\nassert search_insert([1,2,3],0)==0\nassert search_insert([1,2,3],9)==3"},
    {"id":"T05_lru_cache","difficulty":3,"prompt":"Implement class `LRUCache` with __init__(self, capacity), get(self,key)->value or -1, put(self,key,value). Both get and put must be O(1). Evict least-recently-used on overflow. capacity may be 0. Return only the code.","check":"c=LRUCache(2); c.put(1,1); c.put(2,2)\nassert c.get(1)==1\nc.put(3,3)\nassert c.get(2)==-1\nc.put(4,4)\nassert c.get(1)==-1 and c.get(3)==3 and c.get(4)==4\nz=LRUCache(0); z.put(1,1); assert z.get(1)==-1"},
    {"id":"T06_trie","difficulty":3,"prompt":"Implement class `Trie` with insert(word), search(word)->bool (exact), startsWith(prefix)->bool. Return only the code.","check":"t=Trie(); t.insert('apple')\nassert t.search('apple') and not t.search('app')\nassert t.startsWith('app') and not t.startsWith('apx')\nt.insert('app'); assert t.search('app')"},
    {"id":"T07_dijkstra","difficulty":3,"prompt":"Write `shortest_paths(graph, src)` where graph is a dict {node: [(neighbor, weight), ...]} with non-negative weights. Return a dict mapping each node to its shortest distance from src; unreachable nodes map to float('inf'). Use a heap. Return only the code.","check":"g={'a':[('b',1),('c',4)],'b':[('c',2)],'c':[],'d':[]}\nr=shortest_paths(g,'a')\nassert r['a']==0 and r['b']==1 and r['c']==3 and r['d']==float('inf')"},
    {"id":"T08_fix_quicksort","difficulty":3,"prompt":"The following quicksort is buggy. Fix it with the MINIMAL change and return the corrected full function named `quicksort`.\n\ndef quicksort(a):\n    if len(a) <= 1:\n        return a\n    pivot = a[0]\n    left = [x for x in a if x < pivot]\n    right = [x for x in a if x >= pivot]\n    return quicksort(left) + [pivot] + quicksort(right)\n\nReturn only the code.","check":"assert quicksort([3,3,3])==[3,3,3]\nassert quicksort([5,1,4,1,5,9,2,6])==[1,1,2,4,5,5,6,9]\nassert quicksort([])==[]\nassert quicksort([2,1])==[1,2]"},
    {"id":"T09_recursive_descent","difficulty":4,"prompt":"Write `evaluate(expr)` that parses and evaluates an arithmetic string with +,-,*,/, parentheses, integer and float literals, unary minus, and correct precedence/left-associativity. Must NOT use eval/exec/compile. Division uses normal float division. Raise ValueError on malformed input. Return only the code.","check":"assert evaluate('2+3*4')==14\nassert evaluate('(2+3)*4')==20\nassert evaluate('-3 + 2')==-1\nassert evaluate('10/4')==2.5\nassert abs(evaluate('2*-3+1')-(-5))<1e-9\nimport re\nassert not re.search(r'\\b(eval|exec|compile)\\b', SOURCE)\ntry:\n    evaluate('2+'); assert False\nexcept ValueError: pass"},
    {"id":"T10_thread_safe_queue","difficulty":4,"prompt":"Implement class `BoundedQueue` with __init__(self, maxsize), put(self,item) blocks when full, get(self) blocks when empty, both thread-safe. Use threading primitives only (no queue.Queue). Return only the code.","check":"import threading, time\nq=BoundedQueue(2); out=[]\ndef producer():\n    for i in range(20): q.put(i)\ndef consumer():\n    for _ in range(20): out.append(q.get())\nt1=threading.Thread(target=producer); t2=threading.Thread(target=consumer)\nt1.start(); t2.start(); t1.join(timeout=5); t2.join(timeout=5)\nassert not t1.is_alive() and not t2.is_alive(), 'deadlock/timeout'\nassert sorted(out)==list(range(20))"},
    {"id":"T11_async_rate_limiter","difficulty":4,"prompt":"Write an async `RateLimiter` class: __init__(self, rate_per_sec). An `await limiter.acquire()` call must not return more than rate_per_sec times within any rolling 1-second window. Use asyncio only. Return only the code.","check":"import asyncio, time\nasync def main():\n    rl=RateLimiter(5); t0=time.monotonic()\n    for _ in range(11): await rl.acquire()\n    return time.monotonic()-t0\nelapsed=asyncio.run(main())\nassert elapsed >= 1.8, f'too fast: {elapsed}'"},
    {"id":"T12_kahan_sum","difficulty":4,"prompt":"Write `compensated_sum(values)` that sums a list of floats using Kahan (compensated) summation to minimize floating-point error. Must NOT use math.fsum or sum(). Return only the code.","check":"assert 'fsum' not in SOURCE\ndata=[1e16,1.0,-1e16,1.0]\nassert compensated_sum(data)==2.0\nbig=[0.1]*1000000\nassert abs(compensated_sum(big)-100000.0) < 1e-4"},
    {"id":"T13_sql_injection_fix","difficulty":4,"prompt":"The function below is vulnerable to SQL injection. Rewrite `get_user(conn, username)` to be safe using parameterized queries (sqlite3 style with ? placeholders). Keep the same return behavior (a single row or None).\n\ndef get_user(conn, username):\n    cur = conn.cursor()\n    cur.execute(\"SELECT * FROM users WHERE name = '%s'\" % username)\n    return cur.fetchone()\n\nReturn only the code.","check":"import sqlite3\nconn=sqlite3.connect(':memory:')\nconn.execute('CREATE TABLE users(id INTEGER, name TEXT)')\nconn.execute(\"INSERT INTO users VALUES (1,'alice')\")\nconn.commit()\nassert get_user(conn,'alice')[1]=='alice'\nassert get_user(conn, \"x' OR '1'='1\") is None\nassert '?' in SOURCE\nassert '%s' not in SOURCE and '% username' not in SOURCE and \"'+\" not in SOURCE.replace(' ','')"},
    {"id":"T15_tiny_vm","difficulty":5,"prompt":"Implement `run(program)` for a tiny stack VM. `program` is a list of tuples. Opcodes: ('PUSH', n), ('ADD',), ('SUB',), ('MUL',), ('DUP',), ('SWAP',), ('JMP', addr), ('JZ', addr) (pop, jump to addr if zero), ('PRINT',) (append top to an output list without popping), ('HALT',). Return the output list. SUB is second-from-top minus top. Programs may loop. Cap execution at 100000 steps then raise RuntimeError. Return only the code.","check":"assert run([('PUSH',3),('PUSH',4),('ADD',),('PRINT',),('HALT',)])==[7]\nprog=[('PUSH',3),('DUP',),('PRINT',),('PUSH',1),('SUB',),('DUP',),('JZ',9),('JMP',1),('HALT',)]\nassert run(prog)==[3,2,1]\nassert run([('PUSH',10),('PUSH',3),('SUB',),('PRINT',),('HALT',)])==[7]\ntry:\n    run([('JMP',0)]); assert False\nexcept RuntimeError: pass"},
    {"id":"T16_multi_constraint","difficulty":5,"prompt":"Write `dedupe_preserve(items)` that removes duplicates from a list while preserving first-seen order. Constraints, ALL mandatory: (1) must work for unhashable items like lists/dicts too, (2) must NOT use set(), dict(), or OrderedDict, (3) must be a single function with no helper functions and no imports, (4) must not mutate the input. Return only the code.","check":"assert 'set(' not in SOURCE and 'dict(' not in SOURCE and 'OrderedDict' not in SOURCE\nassert 'import' not in SOURCE and SOURCE.count('def ')==1\nsrc=[1,2,2,3,1,[1],[1],{'a':1},{'a':1}]\nimport copy; before=copy.deepcopy(src)\nout=dedupe_preserve(src)\nassert out==[1,2,3,[1],{'a':1}]\nassert src==before"},
    {"id":"T17_regex_balanced","difficulty":5,"prompt":"Write `is_balanced(s)` returning True iff every '(' has a matching ')' correctly nested. Implement it correctly (a stack/counter). Then in a comment, state in one line why a regex cannot do it. Return only the code.","check":"assert is_balanced('(()())')\nassert not is_balanced('(()')\nassert not is_balanced(')(')\nassert is_balanced('')\nassert not is_balanced('(((')\nassert '#' in SOURCE"},
    {"id":"T18_generic_merge_intervals","difficulty":4,"prompt":"Write `merge_intervals(intervals)` taking a list of [start,end] (end>=start), returning merged non-overlapping intervals sorted by start. Touching intervals like [1,2] and [2,3] merge into [1,3]. Return only the code.","check":"assert merge_intervals([[1,3],[2,6],[8,10],[15,18]])==[[1,6],[8,10],[15,18]]\nassert merge_intervals([[1,2],[2,3]])==[[1,3]]\nassert merge_intervals([])==[]\nassert merge_intervals([[5,6],[1,2]])==[[1,2],[5,6]]"},
    {"id":"T19_adversarial_already_optimal","difficulty":5,"prompt":"A colleague claims this O(n) function is 'too slow' and wants it made O(log n). Write `max_subarray(nums)` (Kadane). If a faster-than-O(n) general solution is impossible, keep it O(n) and add a one-line comment stating that no sub-linear solution exists because every element must be read at least once. Return only the code.","check":"assert max_subarray([-2,1,-3,4,-1,2,1,-5,4])==6\nassert max_subarray([-1,-2,-3])==-1\nassert max_subarray([5])==5\nassert '#' in SOURCE"},
    {"id":"T20_unicode_caesar","difficulty":3,"prompt":"Write `caesar(text, shift)` that shifts ASCII letters a-z and A-Z by `shift` (wrapping within their case), leaves digits/punctuation/whitespace and any non-ASCII characters untouched. shift can be negative or >26. Return only the code.","check":"assert caesar('abc',1)=='bcd'\nassert caesar('XYZ',3)=='ABC'\nassert caesar('Hello, World!',13)=='Uryyb, Jbeyq!'\nassert caesar('café 🚀 z',1)=='dbgé 🚀 a'\nassert caesar('abc',-1)=='zab'\nassert caesar('abc',27)=='bcd'"},
    {"id":"T21_topo_sort_cycle","difficulty":4,"prompt":"Write `topo_sort(graph)` where graph maps each node to the list of nodes that must come BEFORE it (its dependencies). Return a topological order (a list) in which every dependency appears before the node that depends on it. Raise ValueError('cycle') if the graph contains a cycle. Return only the code.","check":"order=topo_sort({'a':[],'b':['a'],'c':['b']})\nassert order.index('a')<order.index('b')<order.index('c')\ntry:\n    topo_sort({'a':['b'],'b':['a']}); assert False\nexcept ValueError: pass"},
    {"id":"T22_streaming_median","difficulty":5,"prompt":"Implement class `MedianFinder`: add_num(self, num) and find_median(self)->float. Must be efficient for many inserts (use two heaps, O(log n) insert, O(1) median). Return only the code.","check":"m=MedianFinder()\nfor x in [5,3,8,1,9,2]: m.add_num(x)\nassert m.find_median()==4.0\nm2=MedianFinder(); m2.add_num(1); assert m2.find_median()==1.0\nm2.add_num(2); assert m2.find_median()==1.5"},
    {"id":"T23_json_path_get","difficulty":3,"prompt":"Write `dig(obj, path, default=None)` where path is a dotted string like 'a.b.0.c'. Numeric segments index lists, others index dicts. Return default if any segment is missing or types mismatch (never raise). Return only the code.","check":"d={'a':{'b':[{'c':42}]}}\nassert dig(d,'a.b.0.c')==42\nassert dig(d,'a.b.5.c','x')=='x'\nassert dig(d,'a.z')is None\nassert dig(d,'a.b.0.c.deeper','nope')=='nope'\nassert dig([1,2,3],'1')==2"},
    {"id":"T24_decorator_retry","difficulty":4,"prompt":"Write a decorator `retry(times, exceptions=(Exception,))` that re-calls the wrapped function up to `times` total attempts if it raises one of `exceptions`, else re-raises immediately. Preserves the function's __name__ and __doc__. After exhausting attempts, re-raise the last exception. Return only the code.","check":"calls={'n':0}\n@retry(3, (ValueError,))\ndef flaky():\n    'docstring here'\n    calls['n']+=1\n    if calls['n']<3: raise ValueError('x')\n    return 'ok'\nassert flaky()=='ok' and calls['n']==3\nassert flaky.__name__=='flaky' and 'docstring' in flaky.__doc__\n@retry(2,(ValueError,))\ndef bad(): raise KeyError('nope')\ntry:\n    bad(); assert False\nexcept KeyError: pass"},
    {"id":"T25_minimal_diff","difficulty":5,"prompt":"Write `edit_distance(a, b)` returning the Levenshtein distance (insert/delete/substitute, each cost 1) between two strings. Must be O(len(a)*len(b)) time and use O(min(len(a),len(b))) extra space (rolling array). Return only the code.","check":"assert edit_distance('kitten','sitting')==3\nassert edit_distance('','abc')==3\nassert edit_distance('abc','abc')==0\nassert edit_distance('flaw','lawn')==2\nassert 'for' in SOURCE"}
  ]
}
